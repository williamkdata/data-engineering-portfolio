name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test: 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"

      - run: uv sync
      - run: uv run pytest

      - name: Validate dbt syntax
        run: uv run dbt parse --project-dir dbt --target dev